# test.json 시각적 구조 가이드

## 🎯 한눈에 보는 전체 구조

```
test.json (13MB, 296,131 lines)
│
├─ 📄 메타데이터
│   ├─ name: "공통 화면설계서"
│   ├─ lastModified: "2025-12-15T02:03:21Z"
│   ├─ thumbnailUrl: "https://..."
│   ├─ version: "2297636521254742852"
│   └─ role: "viewer"
│
└─ 🌳 nodes
    └─ "34:2749" (노드 ID)
        └─ 📊 document (CANVAS)
            └─ 📦 children[] (1,657개의 GROUP)
                └─ 🖼️ 화면 설계 트리
```

---

## 🏗️ 화면 단위 구조 (AUTO_0002 예시)

```
┌─────────────────────────────────────────────────────────────┐
│ Group 154 (최상위 화면 그룹)                                  │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Group 142 (화면 헤더)                                    │ │
│ │ ├─ Rectangle 1 (배경)                                   │ │
│ │ └─ TEXT: "AUTO_0002" ⭐ 화면 ID                        │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Group 130 (화면 ID 영역)                                 │ │
│ │ └─ TEXT: "화면 ID" → Label                             │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ screen id (화면 ID 값 영역)                              │ │
│ │ └─ TEXT: "-" → Value                                    │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Group 131 (작성일 영역)                                  │ │
│ │ └─ TEXT: "작성일" → Label                               │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ date (작성일 값 영역)                                     │ │
│ │ └─ TEXT: "25.09.12" → Value                             │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Group 132 (Screen Information 영역)                      │ │
│ │ └─ TEXT: "Screen Information" → Label                   │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ FRAME: "screen info" (정보 값 영역)                      │ │
│ │ └─ TEXT: "프리셋 기능이 추가된 케이스" → Value           │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Group 133 (Description 영역)                             │ │
│ │ └─ TEXT: "Description" → Label                          │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ GROUP: "description" (설명 컨테이너)                     │ │
│ │ └─ FRAME: "full_list" ⭐ 상세 설명                     │ │
│ │     ├─ TEXT: "1" (번호)                                 │ │
│ │     └─ TEXT: "emptycase\n\n구성 : 파일 업로드..."       │ │
│ │             └─ Value (긴 상세 설명, 756px 높이)          │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔍 Label-Value 패턴 상세

### 패턴 A: 단순 Text-Text
```
┌─────────────────────┐
│ TEXT (Label)        │
│ "화면 ID"           │  ← characters: "화면 ID"
└─────────────────────┘
         ↓
┌─────────────────────┐
│ TEXT (Value)        │
│ "-"                 │  ← characters: "-"
└─────────────────────┘
```

**코드에서 감지**:
```typescript
if (textNodes[i].characters === "화면 ID") {
  const value = textNodes[i + 1].characters; // "-"
}
```

---

### 패턴 B: Text-Frame
```
┌──────────────────────────┐
│ TEXT (Label)             │
│ "Screen Information"     │  ← characters: "Screen Information"
└──────────────────────────┘
         ↓
┌──────────────────────────┐
│ FRAME: "screen info"     │
│ └─ TEXT (Value)          │
│    "프리셋 기능이..."     │  ← characters: "프리셋..."
└──────────────────────────┘
```

**코드에서 감지**:
```typescript
// Label 다음 노드가 FRAME인 경우
if (textNodes[i].characters === "Screen Information") {
  // FRAME의 첫 번째 TEXT 자식 찾기
  const frameNode = findNextFrame(nodes[i]);
  const value = frameNode.children[0].characters;
}
```

---

### 패턴 C: Group-full_list
```
┌──────────────────────────┐
│ TEXT (Label)             │
│ "Description"            │  ← characters: "Description"
└──────────────────────────┘
         ↓
┌──────────────────────────┐
│ GROUP: "description"     │
│ └─ FRAME: "full_list"    │  ⭐ 특수 컨테이너
│     ├─ TEXT: "1"         │
│     ├─ TEXT: "긴 내용"    │  ← characters: "emptycase\n..."
│     ├─ TEXT: "2"         │
│     └─ TEXT: "긴 내용"    │
└──────────────────────────┘
```

**코드에서 감지**:
```typescript
// full_list 노드 우선 검색
const fullListNodes = textNodes.filter(node =>
  node.name.includes('full_list') ||
  node.parentName.includes('full_list')
);

// 번호가 아닌 긴 텍스트만 추출
const descriptions = fullListNodes
  .filter(node => node.characters.length > 10)
  .map(node => node.characters)
  .join('\n\n');
```

---

## 📊 노드 타입별 역할

```
┌─────────────┬──────┬─────────────────────────────────────┐
│ 노드 타입   │ 개수 │ 역할                                 │
├─────────────┼──────┼─────────────────────────────────────┤
│ 🎨 CANVAS   │    1 │ 최상위 페이지 (모든 화면의 루트)     │
│ 📦 GROUP    │ 1657 │ 논리적 그룹핑 (화면, 섹션 등)        │
│ 🖼️ FRAME    │  499 │ 레이아웃 컨테이너                    │
│ 📝 TEXT     │ 1423 │ ⭐ 텍스트 내용 (파싱 대상)          │
│ 🔲 RECTANGLE│ 1425 │ 배경, 구분선 등                      │
│ 🎭 VECTOR   │  436 │ 아이콘, 로고 등                      │
│ 🖱️ INSTANCE │  136 │ 재사용 컴포넌트                      │
│ ⭕ ELLIPSE  │  131 │ 원형 도형                            │
│ 🖼️ IMAGE    │   49 │ 이미지 에셋                          │
└─────────────┴──────┴─────────────────────────────────────┘
```

---

## 🎯 TEXT 노드 해부

```json
{
  "id": "99:527",                    // ← 고유 ID
  "name": "프리셋 기능이...",         // ← Figma에서 설정한 노드 이름
  "type": "TEXT",                    // ← 노드 타입 (TEXT 식별)

  "characters": "프리셋 기능이 추가된 케이스",  // ⭐ 실제 텍스트 내용

  "style": {
    "fontFamily": "Pretendard",      // 폰트
    "fontWeight": 500,               // 두께
    "fontSize": 14,                  // 크기
    "textAlignHorizontal": "LEFT",   // 정렬
    "lineHeightPx": 21               // 줄 높이
  },

  "absoluteBoundingBox": {           // 위치 및 크기
    "x": 3996,
    "y": 2444,
    "width": 468,
    "height": 21
  }
}
```

### 파싱 시 중요한 필드

| 필드 | 용도 | 예시 |
|------|------|------|
| `type` | TEXT 노드 식별 | `"TEXT"` |
| `name` | 노드 이름 (메타) | `"full_list"` |
| `characters` | ⭐ **실제 텍스트** | `"프리셋 기능이..."` |
| `absoluteBoundingBox` | 위치 정보 | `{ x, y, width, height }` |

---

## 🧩 파싱 알고리즘 흐름도

```
┌─────────────────────────────────────────┐
│ 1. JSON 파일 로드                        │
└────────────┬────────────────────────────┘
             ↓
┌─────────────────────────────────────────┐
│ 2. nodes["34:2749"].document 접근       │
└────────────┬────────────────────────────┘
             ↓
┌─────────────────────────────────────────┐
│ 3. document.children 재귀 탐색          │
│    - 모든 TEXT 노드 수집                 │
│    - 부모 이름 추적                      │
└────────────┬────────────────────────────┘
             ↓
┌─────────────────────────────────────────┐
│ 4. TEXT 노드 정렬 (문서 순서)            │
│    textNodes = [                        │
│      { name: "화면 ID", characters: "화면 ID" },    │
│      { name: "-", characters: "-" },    │
│      ...                                │
│    ]                                    │
└────────────┬────────────────────────────┘
             ↓
┌─────────────────────────────────────────┐
│ 5. Label-Value 쌍 감지                  │
│    for i in textNodes:                  │
│      if isLabel(textNodes[i]):          │
│        value = textNodes[i+1]           │
│        pairs.push({ label, value })     │
└────────────┬────────────────────────────┘
             ↓
┌─────────────────────────────────────────┐
│ 6. full_list 노드 우선 처리              │
│    fullListNodes = textNodes.filter(    │
│      node => node.name.includes('full_list')  │
│    )                                    │
└────────────┬────────────────────────────┘
             ↓
┌─────────────────────────────────────────┐
│ 7. 최종 ScreenData 생성                 │
│    {                                    │
│      id: "99:508",                      │
│      name: "AUTO_0002",                 │
│      description: "emptycase...",       │
│      screenInformation: "프리셋...",    │
│      createdDate: "25.09.12"            │
│    }                                    │
└─────────────────────────────────────────┘
```

---

## 🔬 실제 데이터 추출 예시

### 입력: test.json의 일부
```json
{
  "children": [
    {
      "type": "TEXT",
      "name": "화면 ID",
      "characters": "화면 ID"
    },
    {
      "type": "TEXT",
      "name": "-",
      "characters": "-"
    },
    {
      "type": "TEXT",
      "name": "Screen Information",
      "characters": "Screen Information"
    },
    {
      "type": "FRAME",
      "name": "screen info",
      "children": [
        {
          "type": "TEXT",
          "characters": "프리셋 기능이 추가된 케이스"
        }
      ]
    }
  ]
}
```

### 출력: 파싱된 데이터
```typescript
{
  labelValuePairs: [
    {
      label: "화면 ID",
      value: "-"
    },
    {
      label: "Screen Information",
      value: "프리셋 기능이 추가된 케이스"
    }
  ],

  screenId: "-",
  screenInformation: "프리셋 기능이 추가된 케이스"
}
```

---

## 🎨 특수 케이스: full_list 상세

### full_list 내부 구조
```
FRAME: "full_list"
├─ TEXT: "1" (번호 레이블)
│   └─ characters: "1"
│   └─ absoluteBoundingBox.height: 21px
│
├─ TEXT: (상세 내용 1)
│   └─ characters: "emptycase\n\n구성 : 파일 업로드..."
│   └─ absoluteBoundingBox.height: 756px  ⭐ 매우 긴 텍스트
│
├─ TEXT: "2" (번호 레이블)
│   └─ characters: "2"
│   └─ absoluteBoundingBox.height: 21px
│
└─ TEXT: (상세 내용 2)
    └─ characters: "업로드 스켈레톤..."
    └─ absoluteBoundingBox.height: 315px
```

### 필터링 전략
```typescript
// 번호 레이블 제외 (height가 작음)
const descriptions = fullListTextNodes.filter(node =>
  node.absoluteBoundingBox.height > 50  // 50px 이상만
);

// 또는 텍스트 길이로 필터링
const descriptions = fullListTextNodes.filter(node =>
  node.characters.length > 20  // 20자 이상만
);
```

---

## 🚀 성능 최적화 팁

### 1. Early Return
```typescript
// AUTO_ 패턴 매칭 실패 시 즉시 리턴
if (!nodeName.match(/^[A-Z]+_[0-9]+/)) {
  return;
}
```

### 2. 선택적 깊이 탐색
```typescript
// full_list는 2단계까지만 탐색
function collectTextNodes(node, depth = 0, maxDepth = 3) {
  if (depth > maxDepth) return [];
  // ...
}
```

### 3. 필드 선택적 추출
```typescript
// 필요한 필드만 추출
function extractTextNode(node: FigmaNode): LightTextNode {
  return {
    name: node.name,
    characters: node.characters,
    type: node.type
    // style, effects 등은 제외
  };
}
```

---

## 📝 요약 체크리스트

파싱 구현 시 확인사항:

- [ ] `nodes["34:2749"].document` 접근
- [ ] `children` 배열 재귀 탐색
- [ ] TEXT 노드의 `type === "TEXT"` 확인
- [ ] `characters` 필드에서 텍스트 추출
- [ ] 부모 노드의 `name` 추적 (parentName)
- [ ] Label-Value 순차 패턴 감지
- [ ] `full_list` 노드 우선 검색
- [ ] 번호 레이블 필터링 (height 또는 length 기준)
- [ ] 화면 ID 정규식 매칭 (`/^[A-Z]+_[0-9]+(_[0-9]+)?$/`)
- [ ] 결과 데이터 구조화 (ScreenData)

---

## 🎯 최종 목표

**입력**: test.json (296,131줄)
**출력**: 파싱된 화면 데이터 배열

```typescript
[
  {
    id: "99:508",
    figmaId: "99:508",
    name: "AUTO_0002",
    baseId: "AUTO_0002",
    suffix: undefined,
    isParent: true,
    description: "emptycase\n\n구성 : 파일 업로드...",
    screenInformation: "프리셋 기능이 추가된 케이스",
    createdDate: "25.09.12",
    pageName: "[v2.0] 25.11.19",
    thumbnailUrl: "https://..."
  },
  // ... 더 많은 화면들
]
```

이 구조를 정확히 이해하면 Figma 화면 설계서를 완벽하게 파싱할 수 있습니다! 🎉
